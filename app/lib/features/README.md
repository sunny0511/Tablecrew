# features/

One directory per feature (e.g. `tables/`, `crews/`, `discover/`, `profile/`, `trust_safety/`), each a self-contained vertical: `presentation/` (screens, widgets), `application/` (Riverpod providers/notifiers), and references into `data/` for repositories — per `docs/ENGINEERING_GUIDELINES.md`'s repository structure.

**Scaffold note (Milestone F0):** empty. The first features (onboarding, then Create Table) land in Milestones F5–F6 per `docs/IMPLEMENTATION_PLAN.md`. Discover is explicitly out of scope for the Foundation effort.
