# Implementation Plan

**Status:** Living document, v1.0. **Owner:** Staff Engineer / Technical Lead role, TableCrew. **Related docs:** `docs/ROADMAP.md`, `docs/PRD.md`, `docs/FEATURES.md`, `docs/ARCHITECTURE.md`, `docs/DATABASE.md`, `docs/API_SPEC.md`, `docs/ENGINEERING_GUIDELINES.md`, `docs/TESTING.md`, `docs/CI_CD.md`, `docs/DEPLOYMENT.md`, `TASKS.md`, `docs/ARCHITECTURE_READINESS_REVIEW.md`.

## 0. Terminology note (read this first)

This document uses **"Foundation"** to mean the engineering bootstrap effort — repo scaffolding, infrastructure, core data layer, and the first working vertical slice of features — that delivers `docs/ROADMAP.md`'s **Phase 0** (single-city MVP, Crew-first only, Hyderabad). "Foundation" is an *engineering* phase name, not a new product-phase number: it does not replace or compete with `ROADMAP.md`'s Phase 0/1/2/3 naming, `TASKS.md`'s "Immediate next steps," or `docs/PRD.md`'s FR numbering. Where this document says "Milestone F3," it means "the third engineering milestone inside the Foundation effort that ships Phase 0" — not a new numbered product phase. This distinction matters because the knowledge base already has one real, previously-uncaught naming collision (documented in Section 2 below) between how different documents described "Phase 0's" own scope; this document is written to avoid adding a second one.

## 1. Confirmed understanding of the architecture and plan

Every document in `docs/` was read or re-verified across three focused passes (engineering/infrastructure readiness, product-scope buildability, and open-blocker audit) before writing this plan, on top of the seven-dimension architecture readiness review already completed (`docs/ARCHITECTURE_READINESS_REVIEW.md`). What follows is the working model this plan is built on — stated explicitly so any error in it can be caught now, not discovered mid-sprint.

**Product model:** TableCrew is a platform for small-group real-world gatherings, built around three concepts — **Table** (the atomic gathering: lifecycle Proposed→Filling→Confirmed→Happened→Rated, terminal Cancelled reachable from Proposed/Filling/Confirmed; Closed/invite-only or Open/Discover-visible), **Crew** (a persistent friend group with recurring Tables and its own chat), and **Discover** (curated stranger-matching, safety-gated, explicitly out of scope for Foundation/Phase 0). Headcount is a hard 2–8 range with activity-dependent recommended defaults. RSVP states are exactly Going/Requested/Waitlisted/Not Going.

**Foundation/Phase 0 scope, precisely:** account creation and phone (Tier 1) verification, profile and interest selection, Crew creation with persistent chat, Closed-Table creation and full lifecycle, invites (Crew-member multi-select and private capped/expiring link-share — see Section 2's Screen 12 clarification), RSVP/waitlist, per-Table ephemeral chat, host-facing post-Table ratings, push notifications, and the Trust & Safety reporting/blocking/duress pipeline (live from day one, per `ROADMAP.md`'s explicit rationale that this infrastructure must be pressure-tested on lower-stakes Crew disputes before Discover raises the stakes). **Explicitly not in Foundation scope:** Discover, Tier 2 identity verification, Open Tables, bill-splitting/payments, venue partnerships, recurring-ritual automation, TableCrew+ subscription billing.

**Tech stack:** Flutter (Riverpod, GoRouter, Material 3 fully re-themed) client; Firebase (Firestore, Auth, Cloud Functions, FCM, Storage, App Check, Remote Config, Crashlytics, Analytics+BigQuery) backend; Typesense (Discover search — not needed until Phase 1); Stripe (payments — not needed until Phase 1); three fully separate Firebase projects (`tablecrew-dev`/`-staging`/`-prod`), Firestore pinned to the regional location `asia-south1` (Mumbai — decided this pass, Section 2).

**Engineering process:** monorepo (`app/` Flutter client + `functions/` Cloud Functions + `firestore/` rules/indexes), trunk-based development, GitHub Actions CI (lint → unit/widget tests → rules-emulator tests → build → integration tests → internal deploy → staged prod rollout), two-approver review for anything touching security rules or the Trust & Safety Functions directory, `@riverpod` code-gen-only state management, and a testing pyramid (unit/widget/rules-emulator/integration/manual-beta) with named tooling at every layer.

This model is internally consistent and buildable, with the corrections and open items in Section 2 accounted for.

## 2. Technical ambiguities and blockers found, and their resolution

Three verification passes (documented in full detail in this session; summarized here) found one genuine, previously-uncaught contradiction and several smaller gaps. All of the following are now resolved as of this document; nothing below is a silent unresolved gap in the plan that follows.

### 2.1 Resolved this pass

- **Bill-splitting Phase 0-vs-later contradiction (the single most consequential finding).** `docs/ROADMAP.md`, `docs/PRD.md`, and `docs/FEATURES.md` all previously scoped bill-splitting into Phase 0, while `TASKS.md`'s founder-confirmed build order sequenced payments after Discover. **Resolved per your direction: bill-splitting is deferred to Phase 1.** This removes Stripe/UPI payment-rail integration, the India merchant-account question, and the `splitRequests`/dispute flow entirely from Foundation's scope — a real scope reduction, not just a documentation fix. All three contradicting documents have been corrected.
- **Firebase production region.** Previously unstated beyond "a multi-region location matched to launch market" — but no India-inclusive multi-region Firestore location exists. **Decided: `asia-south1` (Mumbai), regional (not multi-region).** This is Foundation's literal first infrastructure step (`TASKS.md` item 1) and was genuinely blocking until named. One real technical consequence worth restating here since it affects the DR posture engineers should build against from day one: a regional location does not have multi-region's cross-region failover property, so a full `asia-south1` outage takes Firestore down too, compounding with the already-known single-region Cloud Functions gap rather than providing independent protection against it. This is a disclosed, accepted risk (see `docs/ARCHITECTURE.md` §7–8), not something Foundation needs to solve — but it should inform on-call/incident-response expectations from launch, not be discovered during a real outage.
- **Three non-equivalent "Phase 0 definition of done" formulations** across `ROADMAP.md`, `PRD.md`, and `TASKS.md` were not contradictory so much as three different, unlabeled checkpoints. Reconciled into a single sequence (earliest smoke-test milestone → closed-beta exit criterion → actual Phase 0→1 advancement gate) documented in `docs/PRD.md` §8. Section 5's milestone plan below is built against the *earliest* of these (the smoke-test milestone), since that's what Foundation is actually building toward first.
- **Screen 12 (Invite & Share Sheet) ambiguity** on how a Crew-less host invites a specific known contact without weakening the Closed-Table privacy model — clarified: native-share the private, capped, expiring invite link to one specific contact. No new contact-picker UI needed; this is now stated explicitly rather than left implicit.

### 2.2 Real engineering gaps, not blocking, addressed by this plan's design (Section 4 recommendations)

- **No documented client-side architecture for the offline mutation queue / idempotency-key persistence** that multiple features (Table creation, Crew creation, ratings) independently need. Left unaddressed, this risks three different hand-rolled implementations. Addressed by Recommendation R1.
- **No golden/visual-regression test tooling named**, despite a highly-specific, opinionated design system. Addressed by Recommendation R2.
- **No Firestore→Material 3 `ColorScheme` slot-mapping decided** — the design tokens cover ~11 palette values, M3's `ColorScheme` has ~30 semantic slots. Addressed by Recommendation R3.
- **Typesense has no local/CI emulation story.** Confirmed **not a Foundation blocker** (Discover is out of scope), but flagged so it doesn't surprise whoever starts Phase 1.
- **No SDK/OS version floors, monorepo tool decision, or backfilled ADRs for already-made architectural decisions** (Riverpod-over-Bloc, Typesense-over-Algolia, trunk-based-over-Git-Flow, GitHub-Actions-over-alternatives, Persona-over-Stripe-Identity — five qualifying decisions per `ENGINEERING_GUIDELINES.md`'s own ADR-worthy criteria, currently zero exist). Addressed by Recommendation R4/R5.

### 2.3 Genuinely open — needs a human, not an engineering decision, and does not block Foundation

Persona's Aadhaar/India-ID coverage, the Indian entity structure, India DPDP Act consent-manager specifics, IT Rules 2021 review, ToS/Privacy Policy execution, general-liability insurance binding, and trademark clearance are all real, tracked, open items (`TASKS.md`, `docs/LEGAL.md`) that gate **public launch**, not the ability to write and test code. None require an engineering decision, and — with the exception of ToS/Privacy Policy, which must be in force before any real user data is collected, even in closed beta — none block Foundation's engineering work. They are not repeated in full here; see `docs/ARCHITECTURE_READINESS_REVIEW.md` and `TASKS.md` for the complete list and classification.

## 3. Recommended improvements before implementation begins

- **R1 — Adopt a single client-side offline-mutation abstraction now, before the second feature needs it.** A `core/` Riverpod provider (e.g., `OfflineMutationQueue`) that generates and persists an idempotency key locally, attempts the call, and retries with the same key on reconnect — wrapped once, reused by every idempotent callable's repository method (Table creation, Crew creation, ratings, and later payments). Building this ad hoc inside the first feature that needs it (Create Table) risks two more slightly-different reimplementations by the time Crew creation and ratings are built by different people.
- **R2 — Add golden/visual-regression testing to the widget-test layer now.** `docs/DESIGN_SYSTEM.md` is unusually specific and opinionated (a distinctive warm palette chosen specifically to read as "not a dating app," a deliberate type pairing, a no-spinner skeleton-pulse convention) and itself names "engineering hard-codes a close-enough value" as a known drift risk, currently caught only by a quarterly manual audit. A lightweight `golden_toolkit`-based suite for the core component library (buttons, cards, chips, RSVP status chips), run in CI, closes this gap cheaply and early, before dozens of screens have been built against undetected drift.
- **R3 — Resolve the Material 3 `ColorScheme` slot-mapping as a documented, one-time decision, not an ad hoc per-screen judgment call.** Map TableCrew's named tokens to the M3 slots they obviously correspond to (primary→terracotta, error→brick, background/surface→the cream family) and derive the remaining ~19 unnamed slots via M3's standard tonal-palette generation from those seed colors — write the resulting mapping into the generated token file itself as the single reference, rather than leaving each engineer to independently decide what fills `tertiaryContainer`.
- **R4 — Backfill five ADRs for decisions already made and justified in prose**, per `ENGINEERING_GUIDELINES.md`'s own stated criteria: Riverpod-over-Bloc, Typesense-over-Algolia, trunk-based-over-Git-Flow, GitHub-Actions-over-alternatives, Persona-over-Stripe-Identity. This is low-effort (the justification prose already exists in `ARCHITECTURE.md`/`CI_CD.md`/`SECURITY.md`) and establishes the ADR discipline from commit one rather than after the guideline has already been silently skipped five times.
- **R5 — Pin initial SDK/tool versions and OS floors explicitly in the first scaffolding PR** (Flutter/Dart SDK, `go_router`, `riverpod_generator`, `very_good_analysis`, Firebase SDKs, Node version for Functions, iOS deployment target, Android `minSdkVersion`) rather than leaving them to accumulate implicitly across early PRs — `ENGINEERING_GUIDELINES.md` already mandates pinning exact versions as ongoing policy; this just makes the *initial* set a deliberate, reviewed choice instead of whatever each engineer's local toolchain happened to have installed.
- **R6 — Sequence Trust & Safety infrastructure alongside the core loop, not after it**, consistent with `ROADMAP.md`'s own stated rationale. Concretely: `reportUser`/`reportTable`/`blockUser`/`triggerDuressSignal`/`createLocationShare` and their UI (Screens 27–29) should land in the same milestone window as Create Table and Crew management, not as a final "safety pass" bolted on before launch — see Milestone F6 below.
- **R7 — Defer Typesense infrastructure entirely; do not scaffold Discover-adjacent code paths during Foundation.** Screen 10 (Create Table)'s full spec includes an Open/Discover-visibility branch and a Tier 2 verification interstitial — correct as the long-term contract, but Foundation should build only the Closed-visibility path. Wiring the dormant branch now (even as a disabled/hidden option) adds real complexity and test surface for a capability three engineering milestones away from being reachable; add it when Phase 1 actually starts.

## 4. Foundation implementation plan: milestones, deliverables, dependencies, order of work

Nine milestones, F0–F8. Estimates assume a small founding engineering team (2–4 engineers) working with the tooling and conventions already specified in `docs/ENGINEERING_GUIDELINES.md`/`docs/CI_CD.md`; they are planning estimates, not commitments. Milestones are mostly sequential where a real dependency exists, with explicitly-noted parallelization opportunities.

| # | Milestone | Key deliverables | Depends on | Est. duration |
|---|---|---|---|---|
| F0 | Environment & tooling bootstrap | Three Firebase projects created (`asia-south1`); monorepo scaffolded per `ENGINEERING_GUIDELINES.md`'s exact tree; CI pipeline stages 1–4 live in GitHub Actions; secrets wired (CI + local-dev `.env` bootstrap); Emulator Suite runnable locally; R4 (5 ADRs) and R5 (version pins) done in this milestone's PRs | — | 1–2 weeks |
| F1 | Core data layer & security rules | Firestore schema for Users (public + `private/profile`), Tables, Crews, RSVPs, `idempotencyKeys` deployed to dev; rules-emulator tests at 100% branch coverage per collection; seed/fixture scripts | F0 | 1 week |
| F2 | Auth & identity (Tier 1) | Phone/OTP sign-up via Firebase Auth; DOB/age-gate (18+) with server-side validation; session lifecycle incl. `revokeSessions`; authenticated app shell | F1 | 1–1.5 weeks |
| F3 | Client foundation | Flutter theme hand-transcribed from `DESIGN_SYSTEM.md` (R3's ColorScheme mapping applied here); GoRouter route table for all in-scope Foundation screens; Riverpod provider skeleton (`core/` + feature folders); R1's `OfflineMutationQueue` built here, before any feature needs it; R2's golden-test harness stood up | F0 (parallelizable with F1/F2) | 1.5–2 weeks |
| F4 | Core API surface: Tables & Crews | `createTable`, `updateTable`, `requestSeat`, `confirmAttendee`, `cancelTable`, `cancelRsvp`, `endTableEarly`; `createCrew`, `addMember`, `removeMember`/`leaveCrew`, `updateCrew` — all with idempotency keys where specified, unit + rules tests | F1, F2 | 2–2.5 weeks |
| F5 | Feature: Onboarding | Screens 1–7 (Splash, Phone Entry, OTP, DOB, Profile Setup, Interest Selection, Notification Priming) end to end against F2/F3 | F2, F3 | 1.5 weeks |
| F6 | Feature: Create Table + minimal Crew primitive + Trust & Safety (parallel track) | Screens 9–13 (Home, Create Table, Venue Picker, Invite & Share Sheet, Table Detail) plus a minimal Crew data primitive (create/add-member, no chat yet — needed because Invite & Share Sheet's Crew-only path depends on it, a hidden dependency the product-level build order doesn't otherwise surface). **In parallel per R6:** Screens 27–29 (Report Flow, Block Confirmation, Trusted Contact/Location Share) against `triggerDuressSignal`/`createLocationShare`/`reportUser`/`blockUser` | F4, F5 | 2.5–3 weeks |
| F7 | Feature: Live Table, Chat, Waitlist, Rating + full Crew management | Screens 14–17 (Live Table Screen incl. duress affordance, Table Chat, Waitlist, Post-Table Rating) and Screens 22–26 (full Crews List, Create Crew, Crew Detail, Crew Chat, Recurring Table Schedule) | F6 | 2.5–3 weeks |
| F8 | Notifications, Account/Settings, and hardening | FCM wiring against `docs/FIREBASE.md` §2.4's taxonomy; Notification Center (Screen 35); Profile/Me and Settings (Screens 32–33); analytics-event audit against `docs/FIREBASE.md`'s canonical table (cross-cutting — verify every milestone F2–F7 actually fired its named events, don't wait until now to instrument); a game-day drill against staging; rules-coverage audit | F5, F6, F7 | 1.5–2 weeks |

**Total estimated Foundation duration: roughly 14–18 weeks** with 2–4 engineers and realistic parallelization (F3 alongside F1/F2; the F6 safety track alongside Create Table). This is intentionally close to but not identical with `ROADMAP.md`'s Months 0–4 Phase 0 timeframe — the difference is headroom for the closed-beta run itself (`TASKS.md`'s 3-Crews/2-Tables/6-week smoke test, then `PRD.md` §8's 50-user/10-Crew closed beta) which happens *after* F8, not inside it.

**Explicitly deferred out of Foundation** (per Section 1/2.1): Data Export/Delete Account (Screen 36) and TableCrew+ Subscription (Screen 34) have real API endpoints already specified but no evidenced Phase 0 requirement forcing them into this plan — build if time allows after F8, otherwise carry into the Phase 1 work that follows Foundation. Discover (Screens 18–21), Bill Split Setup/Payment Detail (Screens 30–31), and Tier 2 identity verification (Screen 8, beyond the entry-point stub Screen 10 will eventually need) are firmly Phase 1 and out of scope for this plan entirely.

## 5. What happens after Foundation

Once F0–F8 ship and the earliest smoke-test milestone (`TASKS.md`: 3 Crews × 2 Tables in 6 weeks) is reached, the next checkpoint is the closed-beta exit criterion in `docs/PRD.md` §8, followed by `docs/ROADMAP.md`'s actual Phase 0→1 advancement gate. Only once that gate is met does Phase 1 work begin — Discover, Tier 2 verification, bill-splitting, and TableCrew+ — which will need its own implementation plan at that time, informed by real Foundation-phase learnings rather than planned speculatively now.
